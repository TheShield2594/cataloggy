# pnpm 11 no longer reads this file — the settings that matter now live in
# pnpm-workspace.yaml (`nodeLinker`, `overrides`, `allowBuilds`). Kept so older
# pnpm versions pick the same layout; change it and `nodeLinker` together.
node-linker=hoisted
